# Database Configuration
# Choose one of the following database URLs:

# PostgreSQL (default for local development on macOS)
DATABASE_URL=postgresql+asyncpg://domingossoares@localhost:5432/items_db

# PostgreSQL with password (for Docker or remote servers)
# DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/items_db

# MySQL (for Docker or local MySQL server)
# DATABASE_URL=mysql+aiomysql://items_user:items_pass@localhost:3306/items_db

# Redis Configuration (for caching)
REDIS_URL=redis://localhost:6379/0

# Cache TTL (Time To Live) in seconds - default: 300 (5 minutes)
CACHE_TTL=300
